# Q-Learning Maze Solver

An agent that learns to navigate a maze from scratch using Q-learning (reinforcement learning) , built with NumPy - NO ML frameworks.

##Status
!! In progress

##Setup
\'\'\'bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
\'\'\'